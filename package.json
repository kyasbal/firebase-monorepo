{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "scripts": {
    "deploy": "lerna run build && npx firebase deploy"
  },
  "dependencies": {
    "firebase-tools": "^6.1.1",
    "husky": "^1.2.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna exec npx lint-staged"
    }
  }
}
